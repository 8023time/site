import type React from 'react';
import { Icon } from './base/index';
import type { IconWrapperProps } from './base/data';

export const _8023timeIcon: React.FC<IconWrapperProps> = (props) => {
  return <Icon icon='/avatar.png' {...props} />;
};
